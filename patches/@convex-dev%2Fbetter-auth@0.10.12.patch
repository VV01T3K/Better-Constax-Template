diff --git a/dist/react-start/index.d.ts b/dist/react-start/index.d.ts
index ce148bdd1b9711912c36361abadaaa7db5bfc50a..66f18e2ad7e802dff1a564a1b286149113b711e9 100644
--- a/dist/react-start/index.d.ts
+++ b/dist/react-start/index.d.ts
@@ -3,6 +3,7 @@ import type { GetTokenOptions } from "../utils/index.js";
 export declare const convexBetterAuthReactStart: (opts: Omit<GetTokenOptions, "forceRefresh"> & {
     convexUrl: string;
     convexSiteUrl: string;
+    fetch?: typeof fetch;
 }) => {
     getToken: () => Promise<string | undefined>;
     handler: (request: Request) => Promise<Response>;
diff --git a/dist/react-start/index.js b/dist/react-start/index.js
index df488b3960c5216aea2b2e46d0ce65f2f3a59e6d..24519b18b359b737562ba344bf62c429acb08267 100644
--- a/dist/react-start/index.js
+++ b/dist/react-start/index.js
@@ -38,7 +38,8 @@ const handler = (request, opts) => {
     const headers = new Headers(request.headers);
     headers.set("accept-encoding", "application/json");
     headers.set("host", new URL(opts.convexSiteUrl).host);
-    return fetch(nextUrl, {
+    const fetchImpl = (opts == null ? void 0 : opts.fetch) ?? fetch;
+    return fetchImpl(nextUrl, {
         method: request.method,
         headers,
         redirect: "manual",
diff --git a/dist/utils/index.d.ts b/dist/utils/index.d.ts
index c9dd3dded66dd1a915c01bf98cc52e03aedc6850..cf6eba272a66c3986d3a24eb8f04e39e5e83ff5a 100644
--- a/dist/utils/index.d.ts
+++ b/dist/utils/index.d.ts
@@ -19,6 +19,7 @@ export declare const requireRunMutationCtx: <DataModel extends GenericDataModel>
 export type GetTokenOptions = {
     forceRefresh?: boolean;
     cookiePrefix?: string;
+    fetch?: typeof fetch;
     jwtCache?: {
         enabled: boolean;
         expirationToleranceSeconds?: number;
diff --git a/dist/utils/index.js b/dist/utils/index.js
index 31e57254fbedb60abe027c5f4defe83674a061b7..337986d7c2e416f6a7f592e435e98aa8031af1a6 100644
--- a/dist/utils/index.js
+++ b/dist/utils/index.js
@@ -39,10 +39,13 @@ export const requireRunMutationCtx = (ctx) => {
     return ctx;
 };
 export const getToken = async (siteUrl, headers, opts) => {
+    const requestHeaders = new Headers(headers);
+    requestHeaders.delete("host");
     const fetchToken = async () => {
         const { data } = await betterFetch("/api/auth/convex/token", {
             baseURL: siteUrl,
-            headers,
+            headers: requestHeaders,
+            customFetchImpl: opts == null ? void 0 : opts.fetch,
         });
         return { isFresh: true, token: data?.token };
     };
diff --git a/src/react-start/index.ts b/src/react-start/index.ts
index 755fe7163a523b6ba9018a6a3235a38790334db6..c4e9c4ef3b2df05b81315b895185f1ced34ba634 100644
--- a/src/react-start/index.ts
+++ b/src/react-start/index.ts
@@ -59,13 +59,17 @@ const parseConvexSiteUrl = (url: string) => {
   return url;
 };
 
-const handler = (request: Request, opts: { convexSiteUrl: string }) => {
+const handler = (
+  request: Request,
+  opts: { convexSiteUrl: string; fetch?: typeof fetch }
+) => {
   const requestUrl = new URL(request.url);
   const nextUrl = `${opts.convexSiteUrl}${requestUrl.pathname}${requestUrl.search}`;
   const headers = new Headers(request.headers);
   headers.set("accept-encoding", "application/json");
   headers.set("host", new URL(opts.convexSiteUrl).host);
-  return fetch(nextUrl, {
+  const fetchImpl = opts.fetch ?? fetch;
+  return fetchImpl(nextUrl, {
     method: request.method,
     headers,
     redirect: "manual",
@@ -79,6 +83,7 @@ export const convexBetterAuthReactStart = (
   opts: Omit<GetTokenOptions, "forceRefresh"> & {
     convexUrl: string;
     convexSiteUrl: string;
+    fetch?: typeof fetch;
   }
 ) => {
   const siteUrl = parseConvexSiteUrl(opts.convexSiteUrl);
diff --git a/src/utils/index.ts b/src/utils/index.ts
index 3868090349272c29d71d597b0edcbcda5d52093b..39089d2168569ba5f0611c27c5ff40f316b93fbb 100644
--- a/src/utils/index.ts
+++ b/src/utils/index.ts
@@ -101,6 +101,7 @@ export const requireRunMutationCtx = <DataModel extends GenericDataModel>(
 export type GetTokenOptions = {
   forceRefresh?: boolean;
   cookiePrefix?: string;
+  fetch?: typeof fetch;
   jwtCache?: {
     enabled: boolean;
     expirationToleranceSeconds?: number;
@@ -113,12 +114,16 @@ export const getToken = async (
   headers: Headers,
   opts?: GetTokenOptions
 ) => {
+  const requestHeaders = new Headers(headers);
+  requestHeaders.delete("host");
+
   const fetchToken = async () => {
     const { data } = await betterFetch<{ token: string }>(
       "/api/auth/convex/token",
       {
         baseURL: siteUrl,
-        headers,
+        headers: requestHeaders,
+        customFetchImpl: opts?.fetch,
       }
     );
     return { isFresh: true, token: data?.token };
